{
  "name": "aura-protocol",
  "version": "1.0.0",
  "description": "Aura Protocol - ZK Credit Passport",
  "scripts": {
    "build": "cd frontend && npm ci --legacy-peer-deps && npm run build",
    "dev": "concurrently \"cd frontend && npm start\" \"cd backend && uvicorn server:app --reload --port 8000\"",
    "start": "cd frontend && npm start"
  },
  "devDependencies": {
    "concurrently": "^7.6.0"
  }
}